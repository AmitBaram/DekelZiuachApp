<UserControl x:Class="DekelApp.Views.MikudLayersView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="800">
    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVis"/>
    </UserControl.Resources>
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="220"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Left sidebar for Mikud Areas selection -->
        <Border Grid.Column="0" Background="White" Margin="0,0,20,0" CornerRadius="16" BorderThickness="1" BorderBrush="#F1F5F9">
            <Grid Margin="15">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                
                <TextBlock Text="Mikud Areas" FontWeight="Bold" FontSize="18" Margin="0,0,0,15"/>
                
                <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
                    <StackPanel>
                        <ListBox ItemsSource="{Binding MikudAreas}" SelectedItem="{Binding SelectedArea}" BorderThickness="0" Background="Transparent">
                            <ListBox.ItemTemplate>
                                <DataTemplate>
                                    <Border Padding="10" CornerRadius="8">
                                        <TextBlock Text="{Binding Name}" FontWeight="Medium"/>
                                    </Border>
                                </DataTemplate>
                            </ListBox.ItemTemplate>
                            <ListBox.ItemContainerStyle>
                                <Style TargetType="ListBoxItem">
                                    <Setter Property="Template">
                                        <Setter.Value>
                                            <ControlTemplate TargetType="ListBoxItem">
                                                <Border x:Name="Border" Background="Transparent" CornerRadius="8" Margin="0,2">
                                                    <ContentPresenter />
                                                </Border>
                                                <ControlTemplate.Triggers>
                                                    <Trigger Property="IsSelected" Value="True">
                                                        <Setter TargetName="Border" Property="Background" Value="{StaticResource LightBlueBrush}"/>
                                                        <Setter Property="Foreground" Value="{StaticResource PrimaryBlueBrush}"/>
                                                    </Trigger>
                                                </ControlTemplate.Triggers>
                                            </ControlTemplate>
                                        </Setter.Value>
                                    </Setter>
                                </Style>
                            </ListBox.ItemContainerStyle>
                        </ListBox>

                        <Button Content="Apply to All" Command="{Binding ApplyToAllCommand}" IsEnabled="{Binding HasSelection}" Margin="0,15,0,0" Style="{StaticResource SecondaryButtonStyle}" Height="40"/>
                    </StackPanel>
                </ScrollViewer>
            </Grid>
        </Border>

        <!-- Main Content (Layers for selected Mikud) -->
        <Grid Grid.Column="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- Header -->
            <Border Grid.Row="0" Margin="0,0,0,20">
                <StackPanel>
                    <TextBlock Text="Mikud Layers" FontSize="26" FontWeight="Bold" Foreground="{StaticResource PrimaryTextBrush}"/>
                    <TextBlock Text="{Binding SelectedArea.Name, StringFormat='{}Available layers for: {0}', TargetNullValue='Please select an area'}" FontSize="14" Foreground="{StaticResource SecondaryTextBrush}"/>
                </StackPanel>
            </Border>

            <!-- Layers Selection Card -->
            <Border Grid.Row="1" Style="{StaticResource ContentCardStyle}" IsEnabled="{Binding HasSelection}">
                <ScrollViewer VerticalScrollBarVisibility="Auto">
                    <StackPanel>
                        <Grid Margin="0,0,0,15">
                            <StackPanel>
                                <TextBlock Text="Orthophoto" FontSize="16" FontWeight="SemiBold" Foreground="{StaticResource PrimaryTextBrush}"/>
                                <TextBlock Text="התצלומים" FontSize="13" Foreground="{StaticResource SecondaryTextBrush}"/>
                            </StackPanel>
                            <CheckBox Style="{StaticResource ToggleSwitchStyle}" HorizontalAlignment="Right" VerticalAlignment="Center" IsChecked="{Binding Orthophoto}"/>
                        </Grid>
                        <Separator Background="#F1F5F9" Margin="0,0,0,15"/>

                        <Grid Margin="0,0,0,15">
                            <StackPanel>
                                <TextBlock Text="DTM" FontSize="16" FontWeight="SemiBold" Foreground="{StaticResource PrimaryTextBrush}"/>
                                <TextBlock Text="הגבהים" FontSize="13" Foreground="{StaticResource SecondaryTextBrush}"/>
                            </StackPanel>
                            <CheckBox Style="{StaticResource ToggleSwitchStyle}" HorizontalAlignment="Right" VerticalAlignment="Center" IsChecked="{Binding DTM}"/>
                        </Grid>
                        <Separator Background="#F1F5F9" Margin="0,0,0,15"/>

                        <Grid Margin="0,0,0,15">
                            <StackPanel>
                                <TextBlock Text="Buildings" FontSize="16" FontWeight="SemiBold" Foreground="{StaticResource PrimaryTextBrush}"/>
                                <TextBlock Text="בניינים גנריים" FontSize="13" Foreground="{StaticResource SecondaryTextBrush}"/>
                            </StackPanel>
                            <CheckBox Style="{StaticResource ToggleSwitchStyle}" HorizontalAlignment="Right" VerticalAlignment="Center" IsChecked="{Binding Buildings}"/>
                        </Grid>
                        <Separator Background="#F1F5F9" Margin="0,0,0,15"/>

                        <Grid Margin="0,0,0,15">
                            <StackPanel>
                                <TextBlock Text="Fences" FontSize="16" FontWeight="SemiBold" Foreground="{StaticResource PrimaryTextBrush}"/>
                                <TextBlock Text="גדרות בתא השטח" FontSize="13" Foreground="{StaticResource SecondaryTextBrush}"/>
                            </StackPanel>
                            <CheckBox Style="{StaticResource ToggleSwitchStyle}" HorizontalAlignment="Right" VerticalAlignment="Center" IsChecked="{Binding Fences}"/>
                        </Grid>
                        <Separator Background="#F1F5F9" Margin="0,0,0,15"/>

                        <Grid Margin="0,0,0,15">
                            <StackPanel>
                                <TextBlock Text="Roads" FontSize="16" FontWeight="SemiBold" Foreground="{StaticResource PrimaryTextBrush}"/>
                                <TextBlock Text="כבישים בתא השטח" FontSize="13" Foreground="{StaticResource SecondaryTextBrush}"/>
                            </StackPanel>
                            <CheckBox Style="{StaticResource ToggleSwitchStyle}" HorizontalAlignment="Right" VerticalAlignment="Center" IsChecked="{Binding Roads}"/>
                        </Grid>
                        <Separator Background="#F1F5F9" Margin="0,0,0,15"/>

                        <Grid Margin="0,0,0,15">
                            <StackPanel>
                                <TextBlock Text="Vegetation" FontSize="16" FontWeight="SemiBold" Foreground="{StaticResource PrimaryTextBrush}"/>
                                <TextBlock Text="צמחייה בתא השטח" FontSize="13" Foreground="{StaticResource SecondaryTextBrush}"/>
                            </StackPanel>
                            <CheckBox Style="{StaticResource ToggleSwitchStyle}" HorizontalAlignment="Right" VerticalAlignment="Center" IsChecked="{Binding Vegetation}"/>
                        </Grid>
                        <Separator Background="#F1F5F9" Margin="0,0,0,15"/>

                        <Grid Margin="0,0,0,15">
                            <StackPanel>
                                <TextBlock Text="Power Lines" FontSize="16" FontWeight="SemiBold" Foreground="{StaticResource PrimaryTextBrush}"/>
                                <TextBlock Text="עמודי חשמל בתא השטח" FontSize="13" Foreground="{StaticResource SecondaryTextBrush}"/>
                            </StackPanel>
                            <CheckBox Style="{StaticResource ToggleSwitchStyle}" HorizontalAlignment="Right" VerticalAlignment="Center" IsChecked="{Binding PowerLines}"/>
                        </Grid>
                    </StackPanel>
                </ScrollViewer>
            </Border>
        </Grid>
    </Grid>
</UserControl>
