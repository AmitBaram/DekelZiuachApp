<UserControl x:Class="DekelApp.Views.GeneralInfoView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Grid.Row="0" Margin="0,0,0,30">
            <StackPanel>
                <TextBlock Text="מידע כללי" Style="{StaticResource PageHeaderStyle}"/>
                <TextBlock Text="הזן את פרטי היחידה והמבצע לניהול הדיווח." Style="{StaticResource PageSubHeaderStyle}"/>
            </StackPanel>
        </Border>

        <!-- Main Card Content -->
        <Border Grid.Row="1" Style="{StaticResource ContentCardStyle}">
            <StackPanel>
                <TextBlock Text="פרטי המבצע" FontSize="18" FontWeight="Bold" Foreground="{DynamicResource PrimaryTextBrush}" Margin="0,0,0,30"/>
                
                <Grid Margin="0,0,0,25">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="30"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <!-- Row 1: Right Column (Operation Name) - In RTL Col 0 is Right -->
                    <StackPanel Grid.Column="0">
                        <TextBlock Text="שם המבצע" FontSize="13" FontWeight="SemiBold" Margin="0,0,0,10" Foreground="{DynamicResource SecondaryTextBrush}"/>
                        <TextBox Style="{StaticResource PillTextBoxStyle}" Text="{Binding OperationName, UpdateSourceTrigger=PropertyChanged}" Height="45"/>
                    </StackPanel>

                    <!-- Row 1: Left Column (Unit Name) - In RTL Col 2 is Left -->
                    <StackPanel Grid.Column="2">
                        <TextBlock Text="שם היחידה" FontSize="13" FontWeight="SemiBold" Margin="0,0,0,10" Foreground="{DynamicResource SecondaryTextBrush}"/>
                        <TextBox Style="{StaticResource PillTextBoxStyle}" Text="{Binding UnitName, UpdateSourceTrigger=PropertyChanged}" Height="45"/>
                    </StackPanel>
                </Grid>

                <StackPanel Width="350" HorizontalAlignment="Left" Margin="0,0,0,25">
                    <TextBlock Text="תאריך" FontSize="13" FontWeight="SemiBold" Margin="0,0,0,10" Foreground="{DynamicResource SecondaryTextBrush}"/>
                    <DatePicker SelectedDate="{Binding Date}" Height="45" VerticalContentAlignment="Center"/>
                </StackPanel>

                <StackPanel>
                    <TextBlock Text="הערות נוספות" FontSize="13" FontWeight="SemiBold" Margin="0,0,0,10" Foreground="{DynamicResource SecondaryTextBrush}"/>
                    <TextBox Style="{StaticResource PillTextBoxStyle}" Text="{Binding GeneralNotes, UpdateSourceTrigger=PropertyChanged}" Height="150" AcceptsReturn="True" TextWrapping="Wrap" VerticalContentAlignment="Top" Padding="15"/>
                </StackPanel>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
